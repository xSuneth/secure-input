{
  "name": "@secure-input/wasm",
  "version": "0.2.0",
  "description": "WASM encryption module for secure-input library",
  "type": "module",
  "main": "./pkg/secure_input_wasm.js",
  "types": "./pkg/secure_input_wasm.d.ts",
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/secure-input"
  },
  "keywords": [
    "wasm",
    "encryption",
    "chacha20",
    "security"
  ],
  "files": [
    "pkg/**/*",
    "README.md"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg --release",
    "build:dev": "wasm-pack build --target web --out-dir pkg --dev",
    "test": "cargo test",
    "clean": "cargo clean && rm -rf pkg"
  }
}
